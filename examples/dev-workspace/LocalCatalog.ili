INTERLIS 2.3;

MODEL LocalCatalog (en) AT "https://example.invalid/dev" VERSION "1" =
  DOMAIN
    LocalIdentifier = TEXT*80;
    myEnum = (a, b);


  TOPIC foo2 =

    CLASS bar =

      asdf : TEXT;
      aaa : 0..3;
      bbb: TEXT;
    END bar;

  END foo2;


END LocalCatalog.
